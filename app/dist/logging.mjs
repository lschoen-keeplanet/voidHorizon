import path from"path";import winston from"winston";export const Logger=winston.Logger;export const LogEntry=winston.LogEntry;export function createLogger(o,t){const{createLogger:e,format:r,transports:n}=winston,s=e({level:"debug",format:r.combine(r.timestamp({format:"YYYY-MM-DD HH:mm:ss"}),r.errors({stack:!0}),r.json()),transports:[new n.File({filename:path.join(o.logs,"debug.log")}),new n.File({filename:path.join(o.logs,"error.log"),level:"error"}),new n.Console({format:r.combine(r.colorize(),r.printf((o=>{let t=`${vtt} | ${o.timestamp} | [${o.level}] ${o.message}`;return o.stack&&(t+="\n"+o.stack),t})))})]});for(let o of t)s.log(o);return s}